import { ModulePlaceholder } from '@/components/ui/ModulePlaceholder';

export default function EventsScreen() {
  return (
    <ModulePlaceholder
      title="Events"
      phase="Coming in Phase 4"
      blurb="Calendar, RSVP, recurrence, capacity, and volunteer slots land here."
    />
  );
}
